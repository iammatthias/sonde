{
	"name": "pds",
	"version": "1.0.0",
	"description": "Cirrus – AT Protocol PDS on Cloudflare Workers",
	"type": "module",
	"private": true,
	"dependencies": {
		"@getcirrus/pds": "^0.10.1"
	},
	"devDependencies": {
		"@cloudflare/vite-plugin": "^1.22.0",
		"@types/node": "^25",
		"vite": "^7.3.1",
		"wrangler": "^4.61.0"
	},
	"scripts": {
		"dev": "vite dev",
		"build": "vite build",
		"deploy": "vite build && wrangler deploy",
		"preview": "vite preview",
		"pds": "pds",
		"wrangler": "wrangler",
		"types": "wrangler types"
	},
	"keywords": [],
	"author": "",
	"license": "MIT"
}
